
var speed = 4;
var X = 4;
var Y = 200;

function setup() { 
  createCanvas(600, 600);
}

function draw() {
  background(255); 
  ellipse(X, Y, 90, 90);
if (X > 400) {
speed = speed - 5;
}
  if (X < 50) {
speed = speed + 5;
  }
  X = X + speed;
}
